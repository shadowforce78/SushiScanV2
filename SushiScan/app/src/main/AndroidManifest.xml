<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SushiScan"
        android:usesCleartextTraffic="true"
        tools:targetApi="28">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SushiScan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Nouvelle activité pour les détails du manga -->
        <activity
            android:name=".MangaDetailsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.SushiScan.NoActionBar" />

        <!-- Nouvelle activité pour la lecture des scans -->
        <activity
            android:name=".ScanReaderActivity"
            android:exported="false"
            android:parentActivityName=".MangaDetailsActivity"
            android:theme="@style/Theme.SushiScan.NoActionBar"
            android:screenOrientation="portrait" />
    </application>

</manifest>